Как запустить chart-ы
В дз секреты хранятся в чартах в зашифрованном виде, поэтому для запуска нужно произвести несколько дополнительных шагов
Скачать `sops` и `gnupg`:
```bash
brew install sops & brew install gnupg
```

